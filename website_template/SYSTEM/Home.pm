package $SYSTEM::Home;
use strict;
use Exporter;
# use Date::Parse;
# use dpl::Context;
# use dpl::System;
# use dpl::Base;
# use dpl::Error;
# use dpl::XML;
# use dpl::Log;
# use dpl::Db::Database;
# use dpl::Db::Filter;
# use dpl::Db::Table;
# use URI::Escape;

use $SYSTEM::Base;
use vars qw(@ISA);
@ISA = qw($SYSTEM::Base);

sub ACTION_default {1;}

1;
